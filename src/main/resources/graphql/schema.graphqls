type Query {
    bookById(id: Int): Book
    booksByName(name: String): [Book!]!
}

type Book {
    id: Int
    name: String
    bookAuthor: Author
    genre: Genre
}

type Author {
    id: Int
    name: String
    surname: String
}

enum Genre {
    COMIC,
    DETECTIVE
    SCI_FI
}